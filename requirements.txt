# ---------------------------------------------------------------- web server
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-dotenv>=1.0
httpx>=0.27

# ---------------------------------------------------------------- retrieval core
numpy>=1.26
fastembed>=0.4.2          # ONNX embeddings + reranker, no torch required

# ---------------------------------------------------------------- document parsers
pymupdf>=1.24             # PDF
python-docx>=1.1          # Word .docx
python-pptx>=1.0          # PowerPoint .pptx
openpyxl>=3.1             # Excel .xlsx / .xlsm
beautifulsoup4>=4.12      # HTML
lxml>=5.2
xlrd>=2.0                 # legacy .xls
extract-msg>=0.48         # Outlook .msg

# ---------------------------------------------------------------- OCR
# Reads text out of pictures: standalone images, scanned PDFs, and images
# embedded in Word / PowerPoint / Excel / PDF files. Pure pip install - unlike
# Tesseract it needs no separate system installer.
#
# The floor is 1.2, not 1.3: every 1.3 and 1.4 release caps itself at Python
# <3.13, so on 3.13 -- what python.org hands out today -- pip can see no
# candidate at all and the whole install dies. 1.2.3 is a py3-none-any wheel,
# installs anywhere, and is the version this project is developed against.
# pip honours Requires-Python, so 3.12 and below still get 1.4.x from this line.
#
# The trailing marker on the line below is what the installers match on to drop
# it and carry on when it will not install; everything except reading text out
# of pictures works without it.
rapidocr-onnxruntime>=1.2    # optional-ocr
pillow>=10.0

# ---------------------------------------------------------------- optional extras
# pytesseract>=0.3.10     # alternative OCR engine (needs the Tesseract binary)
# pywin32>=306            # legacy .doc/.xls/.ppt via an installed MS Office
# sentence-transformers   # alternative embedding backend (pulls torch, ~2 GB)
PyYAML>=6.0
